<?php

const ENTITYSPICE_DANGEROUS_DEBUG = FALSE;

require_once 'config.inc';
require_once 'entityspice.inc';
if (defined('DRUSH_BOOTSTRAP_DRUPAL')) {
  require_once 'entityspice.drush.inc';
}

if (ENTITYSPICE_DANGEROUS_DEBUG) {
  require_once 'dev_datahandler.class.inc';
}

/**
 * Implements hook_permission().
 */
function entityspice_permission() {
  $p['administer entityspice'] = [
    'title' => 'administer entityspice',
  ];
  return $p;
}
